# ?????? ??
spring.application.name=sensor-server

# JSON snake_case -> camelCase
spring.jackson.property-naming-strategy=SNAKE_CASE

# --- Render Postgres ---

# ? URL? ???? "DB?" ??? ? (iot_db_4yip)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d4tvv595pdvs73ee8a70-a.oregon-postgres.render.com:5432/iot_db_4yip}

# ? username? "???" (iot_db_4yip_user)
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:iot_db_4yip_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:2QoBf5RJ19oYaSXWasTPG6vzS9xe1v1E}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Render PORT
server.port=${PORT:8080}

# ==============================
#        MQTT
# ==============================
app.mqtt.broker-url=${APP_MQTT_BROKER_URL:tcp://165.246.44.72:2025}
app.mqtt.client-id=${APP_MQTT_CLIENT_ID:sensor-server-backend}
app.mqtt.topic=${APP_MQTT_TOPIC:Bin/test/data}
